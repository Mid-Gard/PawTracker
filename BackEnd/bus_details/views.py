from django.shortcuts import render
from django.http import HttpResponse
# from location.views import getloc
# from geopy.distance import geodesic
# import json
# from location.views import bus_location
# def bus_details(request):
    # status =[]
    # Gec_clk = (15.422249402831747, 73.98014018286432)
    # # for i in range(bus_location):

    # loc = bus_location() 
    # if (geodesic(Gec_clk, (loc[0]['lat'],loc[0]['lng'])).meters <= 235):
    #     data ={"Bus Status" : "In Campus" }
    #     status.append(data)

    # else:
    #     data ={"Bus Status" : "Out Campus" }
    #     status.append(data)

    # return HttpResponse(json.dumps(status), content_type="application/json")    




    